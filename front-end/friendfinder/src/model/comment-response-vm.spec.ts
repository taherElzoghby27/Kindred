import { CommentResponseVm } from './comment-response-vm';

describe('CommentResponseVm', () => {
  it('should create an instance', () => {
    expect(new CommentResponseVm()).toBeTruthy();
  });
});
